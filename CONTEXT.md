# CONTEXT.md

## Current State

- Current phase: Phase 9 complete; Phase 10 remains locked.
- Completed: Project skeleton, governance files, config placeholders, source package directories, `.env.example`, requirements, Phase 1 BTC data system with requested interval gap validation, README usage docs, read-only Gamma API client, raw BTC market search, normalized market schema, basic market availability/liquidity filtering, YES/NO price extraction, Phase 2 mock scanner pipeline tests, the `ParsedMarket` domain model, conservative BTC question field parsing, touch-vs-terminal event type distinction, common parser question-form tests, trailing historical volatility estimation, deterministic Monte Carlo GBM probability simulation, bootstrap fat-tail probability simulation, weighted ensemble probability combination, standardized model diagnostics output, Phase 4 offline model pipeline tests, YES/NO edge calculation, YES/NO expected value calculation, quarter-Kelly paper sizing, paper-only risk limit checks, paper-only trade decision assembly, Phase 5 offline strategy pipeline tests, synthetic BTC market generation, weekly synthetic touch market generation, prior-only volatility estimation for backtests, no-future-data guard checks, synthetic touch market resolution, backtest PnL/metrics calculation, Phase 6 offline backtest pipeline tests, paper-only execution records for trade decisions, append-only JSONL paper trade journal storage, offline Markdown daily report generation, append-only prediction/recommendation logging, paper position mark-to-market calculation, manual paper resolution entry with realized PnL, Phase 7 offline paper pipeline tests, read-only `python -m src.main scan`, read-only `python -m src.main model --market-id MARKET_ID`, local `python -m src.main report`, local `python -m src.main paper`, offline `python -m src.main backtest`, read-only `python -m src.main explain --market-id MARKET_ID`, Phase 8 offline CLI pipeline tests, append-only historical prediction records for calibration, Brier score calculation for resolved historical predictions, clipped binary log loss calculation, calibration curve bucket generation, bucket-level realized hit-rate diagnostics, poor-calibration blocking for paper BUY signals, read-only BTC Up/Down market data backtest helpers, a cached Up/Down backtest runner, `python -m src.main updown-backtest`, skipped-window accounting for slow public API pulls, grouped Up/Down reporting by policy/kind, selectable Up/Down entry policy filters, and read-only CLOB price-history Up/Down backtests.
- Current architecture: Modular Python research system with `src/data`, `src/polymarket`, `src/parser`, `src/models`, `src/strategy`, `src/backtest`, `src/paper`, `src/reports`, and `src/utils`.
- Next task: none; Phase 10 is locked and must not be marked `NEXT` without explicit future unlock.
- Prohibitions: No live trading, no wallet connection, no real orders, no real `.env`, no hard-coded secrets, no look-ahead bias.
- Known limits: BTC loader validates empty, duplicate, invalid, and requested fixed-interval gap data but still depends on local `yfinance` availability for live downloads. Gamma-backed CLI commands are read-only and may depend on Gamma/yfinance availability in live runs. Up/Down CLI uses public Gamma/Data API/Coinbase/CLOB endpoints and cache files; CLOB price-history runs are more stable than Data API trades but still need public API availability on first fetch. Entry policy filters only select which observed historical market-price timing rule to simulate; they are not a live-trading signal. Phase 10 live trading remains locked; `TASKS.md` currently has no `NEXT` by decision because the remaining phase is locked.
